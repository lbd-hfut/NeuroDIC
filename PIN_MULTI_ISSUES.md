# PIN multi solver 问题记录

记录日期：2026-08-09
案例：`case/Multi/CylinderDIC`

## 已确认

1. 自标定的世界尺度恢复正常。棋盘格 SfM 边长为
   `0.010140475642`，恢复比例为 `986.147036240`，二者相乘为
   `10.0 mm`。
2. 目前三角化主流程的坐标使用关系正确：参考状态使用
   `(l0, r0)`，变形状态使用 `(l1, r1)`，三维位移为两次三角化结果之差。
3. `cam_0__cam_1` 与最近真值三维位移的 RMS 约为 `0.0666 mm`、95 分位约
   `0.145 mm`；除个别 pair 外，其余 pair 的 RMS 约为 `0.028–0.077 mm`。
   因此不能将当前全部变形形貌异常归因于三角化公式或世界尺度。

## 待解决问题

### 1. `cam_2__cam_3` 存在明显的稀疏三维异常点

- 该 pair 与最近真值三维位移的 RMS 约为 `0.655 mm`，显著高于其他 pair。
- 其 95 分位误差约为 `0.316 mm`；异常主要表现为少量极端点，而非整体刚性偏差。
- 重投影误差的 95 分位约为 `0.184 px`。较小的重投影误差并不能证明左右匹配
  对应正确，因此仍需回溯对应点和四个二维场。

需要逐点定位大三维位移/误差对应的 `l0`、`r0`、`l1`、`r1` 像素坐标，检查：

- 参考左右视差与变形左右视差是否在该区域发生错误对应；
- 左相机时间位移场是否与右相机时间位移场一致；
- `cam_2` / `cam_3` 的相对标定、基线方向及有效重叠区域是否更敏感。

### 2. 融合后的原始三维形貌仍含离群点

当前融合后处理已经包含：

- 全局体素去重（跨 pair 保留重投影误差更小的候选点）；
- 基于局部点间距和局部切平面残差的三维几何离群点清洗。

最近一次对已有 pair 结果直接重新融合时，体素去重后为 `77,796` 点，几何清洗移除
`11,926` 点，保留 `65,870` 点。该操作不会重新计算二维场。

这只能改善最终展示与融合稳定性，不能替代对 pair 级错误对应/三角化输入质量的排查。

### 3. 二维场质量与三维结果需要分开诊断

二维 `disp` 图视觉上较合理时，三维结果仍可能因以下因素恶化：

- 左右视差中很小的局部对应误差经三角化被放大；
- 两个时刻的左右视差误差相减，导致三维位移出现稀疏跳变；
- 点位靠近低视差、遮挡边缘或 ROI 边界，深度条件数较差。

因此下一轮诊断应同时输出每个 pair 的视差、时间位移、重投影误差、深度/三角化夹角
以及三维位移，并按异常点位置联动可视化，而不只观察二维位移彩图。

## 当前流程与配置约定

- multi 流程仅确定 pair 的参考图、变形图和 ROI；二维求解参数统一读取
  `config/pin_2d.yaml`。
- 最近准备测试的二维参数为：`fourier_encoding.enabled: false`，初始化策略为
  `sift_search`。该配置是否改善三维结果，需在完成相应 pair 结果后单独对比，不能与
  旧结果混为一谈。

## 建议的后续排查顺序

1. 为每个 pair 导出异常三维点及其四幅图像坐标、四个二维场值、重投影误差和三角化夹角。
2. 优先分析 `cam_2__cam_3`，确认异常是左右匹配、时间场、标定几何还是边界/遮挡造成。
3. 基于诊断结果增加 pair 级置信度筛选（例如三角化夹角、前后深度一致性或场一致性），
   再评估融合前的三维质量。
4. 保留现有融合几何清洗作为最终后处理，但以 pair 级问题定位和修复为主。

## 相关产物

- Pair 结果：`case/Multi/CylinderDIC/result/pin_multi_slover/pairs/`
- 融合摘要：`case/Multi/CylinderDIC/result/pin_multi_slover/fused/summary.json`
- 可视化：`case/Multi/CylinderDIC/visualization/pin_multi_slover/`
